# --- Database Mode ---
# Use 'json' for local development with db.json
# Use 'firestore' for production with Firebase Firestore
DATABASE_MODE=json

# --- JWT Secret ---
# A strong, secret key for signing JWT tokens
JWT_SECRET=your-super-secret-key-for-jwt-and-it-should-be-long

# --- Logging Configuration ---
# LOG_TYPE can be 'DEV' or 'PROD'. 
# DEV mode provides detailed logging including potential body logging.
LOG_TYPE=DEV
# LOG_VERBOSITY sets the level of detail in logs. Options: ERROR, WARN, INFO, DEBUG
# DEBUG shows the most detail, ERROR only shows errors
LOG_VERBOSITY=DEBUG
# LOG_FILE is optional. If specified, logs will be written to this file in addition to console.
# Example: LOG_FILE=./logs/app.log
LOG_FILE=

# --- Firebase Configuration ---
# (Keep your existing Firebase variables here)
FIREBASE_PROJECT_ID=your-actual-project-id
FIREBASE_CLIENT_EMAIL=your-service-account-email@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
FIREBASE_DATABASE_URL=https://your-project-id.firebaseio.com